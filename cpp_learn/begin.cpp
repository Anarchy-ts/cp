#include<bits/stdc++.h>
/*
Instead of calling all the individual libraries one by one, 
we can use #include<bits/stdc++.h> to load all the standard cpp
libraries required for competitive programming.
*/

// #include<iostream>
// #include<math.h>
using namespace std; //we'll be using this line for all the cpp programs
/*
We are writing using namespace std as its the standard library containing
cout, cin sqrt, sin etc. if we didnt specify it here we would have to write
std::cout ; std::cin ; std::sqrt(16) in this manner which is quite hectic.
*/
int main(){ //using void main() is no longer valid in cpp; however it may work in older compilers.
    cout<<"Hello";
    cout<< sqrt(16);

    return 0;
}
